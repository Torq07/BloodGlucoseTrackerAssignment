<h1>Users</h1>

<div class='row m-2'>
  <table class="table users">
    <thead class='thead-dark'>
      <tr>
        <th scope="col">#</th>
        <th scope="col">FirstName</th>
        <th scope="col">LastName</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr class='my-2'>
          <th scope="row"><%= user.id %></th>
          <td><%= user.firstname %></td>
          <td><%= user.lastname %></td>
          <td> <%= link_to "Add Level", user %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <div type="button" class="btn btn-outline-primary">
    <%= link_to "New user", new_user_path, :class => "btn"%>
  </div>
</div>

